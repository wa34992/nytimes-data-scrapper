tasks:
  Default:
    command:
      - python
      - task.py


preRunScripts:
  - pip install -r requirements.txt

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore